import { ReactNode } from 'react';
type Props = {
    heading: string;
    close: () => void;
    children?: ReactNode;
    className?: string;
};
declare const Modal: ({ heading, close, children, className }: Props) => import("react").ReactPortal | null;
export { Modal };
export type { Props as ModalProps };
